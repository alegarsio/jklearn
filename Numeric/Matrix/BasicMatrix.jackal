class BasicMatrix {

    init(data : Array){
        this.data = data
    }

    func transpose(){
        this.data = native_transpose(this.data)
        return this
    }

    func dot(otherArray){
        this.data = __matrix_dot(this.data,otherArray)
        return this
    }

    func correlate(otherArray) {
        let score = native_correlate(this.data, otherArray)
        return score
    }

    func exports(){
        return this.data
    }

}